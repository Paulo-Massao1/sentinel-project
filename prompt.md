Fix: The "← Back" link is centered on the page. It should be aligned to the top-left corner of the page, outside the centered content container. Like a standard website back/navigation link. Apply this to all pages that have a Back link (CaseDetail, NewCase, Identify).